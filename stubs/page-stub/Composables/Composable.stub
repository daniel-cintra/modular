import { ref } from 'vue'

export default function use{{ ComposableName }}() {
    const foo = ref('bar')

    const updateFoo = (value) => {
        foo.value = value
    }

    return { updateFoo }
}
