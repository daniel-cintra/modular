import { ref } from 'vue'

export default function use{{ ComposableName }}() {
    const title = ref('foo')

    const {{ composableName }} = () => {
        return title.value
    }

    return { {{ composableName }} }
}
